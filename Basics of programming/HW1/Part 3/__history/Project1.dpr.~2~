program Project1;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

var k : integer; r, s, eps : real;

begin
  write('Введите точность: '); readln(eps);
  k := 0;
  s := 0;
  repeat
    k := k + 1;
    if ((k - 1) mod 2 = 0) then r := 1 / k
    else r := -1 / k;
    s := s + r;
  until (abs(s - ln(2)) < eps);
  writeln('Сумма с заданной точностью равна ', s:16:16);
  writeln('Количество итераций равно ', k);
  readln;
end.
